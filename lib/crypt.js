/**
 * Шифруем пароль, когда нибудь
 * @function
 */
const cryptPassword = password => password;

/**
 * Расшифруем пароль, когда нибудь
 * @function
 */
const unCryptPassrow = password => password;

module.exports = {
  cryptPassword,
  unCryptPassrow,
};
