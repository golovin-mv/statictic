const { expect } = require('chai');
const { getAll } = require('./jobs');

describe('jobs', () => {
  it('should get jobs info', () => {
    // TODO wwite tests
    expect(false).to.be.false;
  })
});